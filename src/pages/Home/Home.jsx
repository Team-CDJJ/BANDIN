import { Link } from 'react-router-dom';
// import Splash from '../Splash/Splash';

const Home = () => {
  return (
    <div>
      <Link to='/login'>로그인</Link>
    </div>
  );
};

export default Home;
